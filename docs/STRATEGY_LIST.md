# 📋 策略清单与使用指南

## 📊 系统当前支持的策略（11个）

### ✅ 趋势跟踪策略（4个）

| 策略ID | 中文名称 | 风险等级 | 说明 |
|--------|---------|---------|------|
| `ma_cross` | 双均线交叉 | 中 | 短期均线上穿长期均线买入，下穿卖出 |
| `macd` | MACD策略 | 中 | MACD金叉买入，死叉卖出 |
| `momentum` | 动量策略 | 高 | 追涨杀跌，基于价格和成交量动量 |
| `breakout` | 突破策略 | 高 | 突破近期高低点进行交易 |

### ✅ 均值回归策略（4个）

| 策略ID | 中文名称 | 风险等级 | 说明 |
|--------|---------|---------|------|
| `rsi` | RSI超买卖 | 中 | RSI超卖买入，超买卖出 |
| `bollinger` | 布林带突破 | 中 | 价格突破上轨买入，跌破下轨卖出 |
| `mean_reversion` | 均值回归 | 中 | 价格偏离均值后回归，适合震荡市场 |
| `kdj` | KDJ随机指标 | 中 | 基于KDJ指标的交叉和超买超卖 |

### ✅ 其他策略（3个）

| 策略ID | 中文名称 | 风险等级 | 说明 |
|--------|---------|---------|------|
| `volume` | 成交量策略 | 中 | 基于成交量变化的量价分析 |
| `multi_factor` | 多因子组合 | 低 | 综合MA、MACD、RSI等多个因子 |
| `grid` | 网格交易 | 低 | 在价格区间内低买高卖，适合震荡市场 |

---

## 🎯 策略使用方式

### 1. 在 Python 代码中使用

```python
from strategy.engine import StrategyEngine

engine = StrategyEngine()

# 获取单个策略
strategy = engine.get_strategy('ma_cross')
signal = strategy.get_current_signal(df)

# 使用多个策略分析
results = engine.analyze(df, ['ma_cross', 'macd', 'rsi'])
```

### 2. 在 Web 界面中使用

侧边栏会显示所有可用策略，可以勾选多个策略进行综合分析。

### 3. 在配置文件中使用

查看 `config/strategies.yaml` 了解策略参数配置。

---

## ❗ 常见问题

### 问题1: "未知策略" 错误

**错误信息**:
```
ValueError: 未知策略: 'xxx'
可用策略: ma_cross, macd, rsi, ...
```

**解决方案**:
1. 检查策略ID拼写是否正确
2. 从上面的策略清单中选择正确的策略ID
3. 使用 `engine.get_strategy_list()` 查看所有可用策略

### 问题2: 策略验证失败

**解决方案**:
```python
# 使用 validate_strategies 方法验证
valid, invalid = engine.validate_strategies(['ma_cross', 'invalid_id'])
print(f"有效策略: {valid}")
print(f"无效策略: {invalid}")
```

### 问题3: 策略返回 ERROR

**检查项**:
1. 数据是否足够（通常需要至少60天的数据）
2. 数据列是否完整（需要 open, high, low, close, volume）
3. 是否有异常值（NaN 或无穷大）

---

## 🔧 策略开发指南

### 添加新策略的步骤

1. 创建策略文件 `strategy/your_strategy.py`:

```python
from strategy.base_strategy import BaseStrategy

class YourStrategy(BaseStrategy):
    def __init__(self, params=None):
        default_params = {
            'param1': value1,
            'param2': value2,
        }
        if params:
            default_params.update(params)
        super().__init__('YourStrategy', default_params)
    
    def generate_signals(self, df):
        # 实现信号生成逻辑
        result = df.copy()
        # ... 计算指标和信号
        return result
    
    def get_current_signal(self, df):
        # 实现当前信号判断
        # 返回格式：
        return {
            'signal': 'BUY' or 'SELL' or 'HOLD',
            'confidence': 0.0-1.0,
            'reason': '说明原因',
            # 其他字段...
        }
```

2. 在 `strategy/engine.py` 中注册策略:

```python
from strategy.your_strategy import YourStrategy

self.strategies = {
    # ... 其他策略
    'your_strategy_id': YourStrategy,
}
```

3. 在 `get_strategy_info` 中添加策略信息:

```python
strategy_info = {
    # ... 其他策略信息
    'your_strategy_id': {
        'name': '策略中文名',
        'description': '策略描述',
        'category': '策略分类',
        'risk_level': '风险等级',
    },
}
```

4. 更新 Web 界面中的策略列表（如果需要）

---

## 📈 策略分类说明

### 趋势跟踪策略
适合市场：上涨/下跌趋势明显的市场
特点：顺势而为，持仓时间较长
代表：双均线交叉、MACD、动量策略、突破策略

### 均值回归策略
适合市场：震荡市场，价格在区间内波动
特点：高抛低吸，逆势操作
代表：RSI、布林带、均值回归、KDJ

### 量价分析策略
适合市场：成交量有明显变化的行情
特点：基于成交量验证价格走势
代表：成交量策略

### 综合策略
适合市场：各种市场环境
特点：多因子综合判断，稳健型
代表：多因子组合、网格交易

---

## 📝 更新日志

### 2026-02-15
- ✅ 新增 8 个策略（布林带、动量、均值回归、突破、KDJ、成交量、多因子、网格）
- ✅ 策略总数达到 11 个
- ✅ 改进错误处理，提供更友好的错误信息
- ✅ 添加策略验证功能

---

## 🤝 贡献指南

如果你想添加新策略：

1. 确保策略有清晰的交易逻辑
2. 提供完整的参数配置
3. 添加策略说明文档
4. 测试策略在各种市场条件下的表现
5. 提交 Pull Request

---

## 📞 获取帮助

如果遇到策略相关问题：

1. 查看本文档确认策略ID正确
2. 运行测试脚本：`python test_strategies.py`
3. 查看策略详细文档：`docs/STRATEGIES.md`
